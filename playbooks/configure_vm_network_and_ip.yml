---
- name: Cloud | Configure VM Network and IP
  hosts: all
  gather_facts: True
  collections:
    - redhatofficial.cloud_utils
  tasks:
    - import_role:
        name: configure_vm_network_and_ip

